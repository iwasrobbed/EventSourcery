platform :ios, '9.0'

# ignore all warnings from all pods
inhibit_all_warnings!

# use frameworks for Swift
use_frameworks!

target 'EventSourceryTests' do
  pod 'Quick'
  pod 'Nimble'
  pod 'Mockingjay'
end
